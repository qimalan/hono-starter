# Template for environment variables.
# Copy this to .env and fill in your local values.

# Application
NODE_ENV=development
LOG_LEVEL=info
PORT=3000

# Database
# Use 'localhost' when running the app directly on the host machine.
# Use the docker-compose service name (e.g., 'postgresql') when running the app inside a Docker container.
POSTGRES_HOST=localhost
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=5432
DATABASE_URL=
