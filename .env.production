# ----------------------------------------------------
# PRODUCTION-SPECIFIC SETTINGS
# Bun loads this when NODE_ENV is 'production'.
# IMPORTANT: Use your actual production values here.
# For security, these are often set in the hosting environment, not in a file.
# ----------------------------------------------------
NODE_ENV=production

# Keep logs concise in production
LOG_LEVEL=info

# Port for the production server
PORT=8080

# --- PRODUCTION DATABASE ---
# IMPORTANT: Replace with your actual production database credentials.
# These should ideally be injected by your deployment environment.
POSTGRES_HOST=your-production-db-host.com
POSTGRES_USER=prod_db_user
POSTGRES_PASSWORD=a_very_secure_production_password
POSTGRES_DB=prod_db
POSTGRES_PORT=5432

DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
